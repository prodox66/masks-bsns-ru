# Спринт 26-09-07 19:56 MSK

1. [+] Подготовить статический `masks.bsns.ru` для реальных масок с раздельным хранением кода и FTP-контента.
   - Код: автономная страница, единый runtime-config, объектный provider, общий Library Window и Lightbox.
   - Контент: `NewUI/masks/*` исключён из Git; локально подготовлены 114 WebP, `files.js` и 114 lazy data-файлов для загрузки по FTP.
   - Генератор: сканирует фактически найденные изображения и пересобирает индекс и data-файлы.
   - Проверено: JS syntax; байтовый contract 114/114; браузер 3/3, страницы 30/30/30/24, масочный Lightbox, pageerror=0.
   - Пользователь выполнил FTP-залив и явно подтвердил работу новой версии: «сейчас новая работает».

2. [~] Убрать локальный default page size и наследовать общую геометрию Library Window.
   - `mask-gallery.js` использует `context.pageSize`, а при прямом вызове — `BZNNewUILibraryWindow.pageSize()`.
   - Проверено: contract114/114; Chrome3/3, страницы30/30/30/24, белый mask Lightbox.
   - Остаётся: push, server pull и пользовательская проверка после обновления общего UI.
